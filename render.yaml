services:
  - type: web
    name: tegbale-api
    runtime: node
    buildCommand: npm install && npx prisma db push --accept-data-loss && npx prisma generate && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
